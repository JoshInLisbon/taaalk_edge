const header = document.querySelector('.tlk-title');
const headerEdit = document.querySelector('.tlk-title-edit');
const tlkImages = document.querySelectorAll('.tlk-image');

header.innerText = headerEdit.value;
tlkImages.forEach(image => {
  image.style = "background-image: url(<%= rails_blob_url(@tlk.image) %>)";
});
