<%= link_to "Follow #{@user.username}", user_follow_path(@user.id), method: :post, remote: true, class: "user-follow link-txt" %>
<div class="tlk-note tlk-follow-note mx-0">
  <p><%= number_with_delimiter(@user.followers.count + 1, :delimiter => ',') %> follower<%= @user.followers.count > 0 ? "s" : "" %></p>
</div>

